import { OcultarMostrarPipe } from './ocultar-mostrar.pipe';

describe('OcultarMostrarPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarMostrarPipe();
    expect(pipe).toBeTruthy();
  });
});
